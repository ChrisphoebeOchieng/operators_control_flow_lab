# Operators & Control Flow Lab

This repository contains my solutions for the **Operators and Control Flow** lab. The lab focuses on using Python operators and control flow statements to make programs respond to different situations.

## Objectives
- Identify and use common Python operators (`+`, `-`, `*`, `/`, `==`, `>`, `<`, etc.)
- Declare and manipulate variables
- Implement conditional statements (`if`, `elif`, `else`)
- Solve coding scenarios using control flow

## Lab Scenarios
Each Python file corresponds to a specific coding scenario from the lab:

- `question1.py` – Example scenario description
- `question2.py` – Example scenario description
- …and so on.

## How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/yourusername/operators_control_flow_lab.git
